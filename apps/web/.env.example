VITE_API_URL=http://localhost:3000

# Sentry (Fase 9.1 observability) — optional; without VITE_SENTRY_DSN,
# Sentry.init() is a safe no-op and nothing is shipped anywhere external.
# See docs/technical-log/phase-9.md.
# VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
